# VatValidation

VatValidation is a Caspeco library for validating VAT numbers and company registration numbers across EU countries.

## Code style

Code style is enforced by `.editorconfig`. **Never modify `.editorconfig`.**

To fix formatting before committing:
```bash
dotnet format ./VatValidation.sln --verbosity diagnostic --severity info --exclude-diagnostics IDE0130
```
